changes:
- type: fix
  component: general
  description: '`shut mono|pkg update --verify-tag ""` does not show an error when it should'
  fixes: []
- type: feature
  component: general
  description: '`shut mono|pkg update --verify-tag` now accepts strings prefixed with `refs/tags/`
    and strips that prefix (useful for GitHub Actions to use the `$GITHUB_REF` variable)'
  fixes: []
- type: fix
  component: general
  description: '`shut pkg update --dry` option not respected'
  fixes: []
