release_date: null
changes:
- type: change
  component: cli
  description: Updated temrinal output of `shut ... checks`
  fixes: []
- type: change
  component: shut.model.package
  description: Add `PackageError` exception class
  fixes: []
- type: improvement
  component: shut.checkers.monorepo
  description: Add `bad-package-directory` check to Monorepo checker
  fixes: []
- type: improvement
  component: cli
  description: '`shut pkg checks` now inherits checks from the Monorepo if the check
    result targets the package directly (e.g. the `bad-package-directory` check)'
  fixes: []
- type: improvement
  component: shut.checkers.core
  description: 'Add `CheckResult.subject` member (default: `None`)'
  fixes: []
- type: change
  component: cli
  description: '`shut pkg` and `shut mono` commands will now print failed checks to
    stderr before executing the requested subcommand.'
  fixes: []
- type: improvement
  component: shut.checkers.monorepo
  description: include error message in `invalid-package` check result
  fixes: []
- type: improvement
  component: shut.checkers.generic
  description: '`unknown-config-options` check no longer shows if it passes'
  fixes: []
- type: improvement
  component: shut.checkers.generic
  description: rename `unknown-config` check to `unknown-config-options`
  fixes: []
