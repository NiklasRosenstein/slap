release_date: null
changes:
- type: fix
  component: cli
  description: Fix `shut mono bump` not updating requirements in package `setup.py`
    if packages have inter dependencies
  fixes:
  - '#7'
- type: feature
  component: shut.model.package
  description: Add `$.install.index-url` and `$.install.extra-index-urls`
    fields
  fixes: []
- type: feature
  component: cli
  description: '`shut pkg install` now supports `$.install.index-url` and `$.install.extra-index-urls`,
    added a `--pipx` option'
  fixes: []
