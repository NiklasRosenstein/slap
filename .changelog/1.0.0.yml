release_date: null
changes:
- type: improvement
  component: general
  description: Replace `shore.utils.git` with `nr.utils.git` module
  fixes: []
- type: change
  component: cli
  description: default value for `shore changelog --for` option is now `general`
  fixes: []
- type: change
  component: changelog
  description: update changelog entry format (`ChangelogEntryV2`), V1 will be automatically
    converted to V2 on load
  fixes: []
- type: feature
  component: cli
  description: add `-s,--stage` and `-c,--commit` options to `shore changelog` command
  fixes: []
- type: docs
  component: cli
  description: add help to options in `shore bump` command
  fixes: []
- type: change
  component: cli
  description: remove backwards comatibility for old `shore bump` syntax, a relative
    version bump can now only be controlled via the respective options and no longer
    via a positional argument
  fixes: []
